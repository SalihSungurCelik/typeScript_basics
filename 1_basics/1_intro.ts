/**
 * Typscript, js'in açıklarını kapatmak için Microsoft tarafından geliştirilen bir programlama dilidir.
 *
 * Js'de tanımlanan bir değişkene her türde değer ataması yapılabilir.
 *
 * Js'de otomatik tamamlama özelliği yok.
 *
 * js'de kodu çalıştırmadan hataları göremiyouz. TS'de hataları direkt görebiliriz.
 * TS kodları doğrudan çalıştırılamaz. JS kodlarına derlenmesi gerekir.
 */

//1-) Tip koruması (String kendi içinde let ile tanımlandıysa değiştirilebilir ama string olan bir veriye number atanamaz!)

//2-) otomatik tamamlama

// const teacher = {
//   name: "ali",
//   age: 30,
// };

teacher.name;
teacher.age;
// teacher.surname;
